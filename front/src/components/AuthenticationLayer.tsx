import {ReactNode} from "react";

function AuthenticationLayer({children}: { children: ReactNode }) {

    return (
        <>
            {children}
        </>
    )
}

export default AuthenticationLayer
